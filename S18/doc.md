### Вариант 18. Room Equipment Service (Сервис оборудования)

### Добавить оборудование
| Параметр | Пояснение | Обязательность | Тип | Ограничение | Значение по умолчанию |
|---|---|---|---|---|---|
| equipment_id | Названия единиц оборудования | обязательно | Список строк | | |
| room_id | номер кабинета | обязательно | целое число | Больше 0 | |

`is_active` выставляется автоматически при создании оборудования в состояние True.

`equipment_id` создаются в БД автоматически.

Уникальных комбинаций нет.

В случае удачного создания оборудования возвращается:
| Параметр | Тип |
|---|---|
| ids | Список целых чисел |
| names | список строк |
| room_id | целое число |
| is_active | булевое значение |

### Создание оборудования
| Параметр | Пояснение | Обязательность | Тип | Ограничение | Значение по умолчанию |
|---|---|---|---|---|---|
| names | Названия единиц оборудования | обязательно | Список строк | | |

`is_active` выставляется автоматически при создании оборудования в состояние True.

`equipment_id` создаются в БД автоматически.

Уникальных комбинаций нет.

В случае удачного создания оборудования возвращается:
| Параметр | Тип |
|---|---|
| ids | Список целых чисел |
| names | список строк |
| is_active | булевое значение |

### Изменить оборудование кабинета
| Параметр | Пояснение | Обязательность | Тип | Ограничение |
|---|---|---|---|---|
| ids | Названия единиц оборудования | обязательно | Список строк | |
| room_id | id кабинета | обязательно | целое число | Больше 0 |

Список оборудования полностью перезаписывается.

В случае удачного изменения списка оборудования возвращается:
| Параметр | Тип |
|---|---|
| ids | Список целых чисел |
| names | Список строк |
| room_id | целое число |
| is_active | булевое значение |


### Удалить оборудование по id оборудования
Данные из БД не удаляются, удалённым (неактивным) данным присваивается статус `is_active` в значение False

Вернёт, если оборудование было успешно удалено
| Параметр | Тип | Сообщение |
|---|---|---|
| message | Строка | Оборудование удалено |

Вернет ошибку, если оборудование не найдено или уже удалено
| Параметр | Тип | Сообщение |
|---|---|---|
| message | Строка | Не удалось удалить |


### Получить оборудование по id кабинета
Информация возвращаемая в случае удачного получения в виде списка всего оборудования кабинета:
| Параметр | Пояснение | Тип |
|---|---| --- |
| equipment_id |  id оборудования | целое число |
| equipment_name | название оборудования | строка |
| room_id | id кабинета | целое число |
| is_active | статус | булевое значение |


### Получить список оборудования по параметрам
| Параметр | Пояснение | Тип |
|---|---|---|
| room_id | Частичный поиск | целое число |
| equipment_ids | Поиск по одному или нескольким id оборудования | список целых чисел |
| is_active | Поиск по статусу доступности оборудования | булевое значение |

Информация возвращаемая в случае удачного получения в виде списка оборудования
| Параметр | Тип |
|---|---|
| equipment_ids | Список целых чисел |
| equipment_name | Список строк |
| room_id | целое число |
| is_active | булевое значение |

### ERD
![ER-диаграмма](erd.png)
